spring:
  application:
    name: configserver
  cloud:
    config:
      server:
        git:
          uri: file://Users/bogus/Downloads/config-server-repository
          clone-on-start: true
      fail-fast: true