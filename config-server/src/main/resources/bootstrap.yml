spring:
  application:
    name: configserver
  cloud:
    config:
      server:
        git:
          uri: file://Users/joseescobar/Documents/projects/config-server-repository
          clone-on-start: true
      fail-fast: true